// a change for live server
var app = require('./server-config.js');
var change = 'change for live server';
var change2 = 'another change';
var change3 = 'anooooother change';
var port = 4568;

app.listen(port);

console.log('Server now listening on port ' + port);
